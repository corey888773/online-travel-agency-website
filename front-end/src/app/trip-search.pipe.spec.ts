import { TripSearchPipe } from './trip-search.pipe';

describe('TripSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TripSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
